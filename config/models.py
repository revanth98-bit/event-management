"""here we will define all the modules baseclasses for the migrations"""
from event_management.models.models import BaseClass

Models = [BaseClass.metadata] # here we should add the imported models inside the list